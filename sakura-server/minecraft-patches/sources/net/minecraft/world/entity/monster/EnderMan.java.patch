--- a/net/minecraft/world/entity/monster/EnderMan.java
+++ b/net/minecraft/world/entity/monster/EnderMan.java
@@ -83,6 +_,7 @@
     private static final UniformInt PERSISTENT_ANGER_TIME = TimeUtil.rangeOfSeconds(20, 39);
     private long persistentAngerEndTime;
     private @Nullable EntityReference<LivingEntity> persistentAngerTarget;
+    private long lastTeleportAttemptTime; // Sakura - fix endermen teleporting causing lag
 
     public EnderMan(final EntityType<? extends EnderMan> type, final Level level) {
         super(type, level);
@@ -296,6 +_,14 @@
     }
 
     private boolean teleport(final double x, final double y, final double z) {
+        // Sakura start - fix endermen teleporting causing lag
+        if (this.tickCount == this.lastTeleportAttemptTime) {
+            return false;
+        }
+
+        this.lastTeleportAttemptTime = this.tickCount;
+        // Sakura end - fix endermen teleporting causing lag
+
         BlockPos.MutableBlockPos pos = new BlockPos.MutableBlockPos(x, y, z);
 
         while (pos.getY() > this.level().getMinY() && !this.level().getBlockState(pos).blocksMotion()) {
